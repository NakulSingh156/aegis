version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/videos:/app/videos
      - ./backend/models:/app/models

  frontend:
    build: ./frontend/staff-dashboard
    ports:
      - "5173:80"
    depends_on:
      - backend
